<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - ChatApp</title>

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  </head>
  <body
    class="bg-gradient-to-r from-blue-500 via-teal-500 to-green-500 min-h-screen flex flex-col justify-between"
  >
    <!-- Main Content (Centered) -->
    <div
      class="flex flex-col justify-center items-center flex-grow text-white text-center px-6 md:px-12"
    >
      <!-- Header Section -->
      <h1 class="text-5xl font-extrabold mb-4 tracking-tight md:text-6xl">
        Login to ChatApp
      </h1>
      <p class="text-lg mb-8 font-light text-gray-200 md:text-xl">
        Enter your credentials to access your account
      </p>

      <!-- Login Form -->
      <form
        action="/api/v1/login"
        method="POST"
        class="w-full max-w-md bg-white p-8 rounded-lg shadow-lg"
      >
        <!-- Email Input -->
        <div class="mb-6">
          <label
            for="email"
            class="block text-gray-700 text-lg font-semibold mb-2"
            > <i class="fa-solid fa-envelope mr-2"> </i>  Email</i></label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full p-4 border-2 border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-black"
            placeholder="Enter your email"
          />
        </div>

        <!-- Password Input -->
        <div class="mb-6">
          <label
            for="password"
            class="block text-gray-700 text-lg font-semibold mb-2"
            ><i class="fa-solid fa-lock mr-2"></i> Password </label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full p-4 border-2 border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-black"
            placeholder="Enter your password"
          />
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full py-4 bg-blue-500 text-white font-semibold rounded-full hover:bg-blue-600 transition duration-300 ease-in-out"
        >
          Log In
        </button>
      </form>

      <!-- Link to Sign Up Page -->
      <p class="mt-6 text-gray-200">
        Don't have an account?
        <a
          href="/signup"
          class="text-blue-400 hover:text-blue-600 transition duration-300"
          >Sign up</a
        >
      </p>
    </div>

    <!-- Footer Section -->
    <footer class="text-center text-gray-200 py-4 mt-auto">
      <p>&copy; 2025 ChatApp. All rights reserved.</p>
    </footer>
    <script>
        document.getElementById("")
    </script>
  </body>
</html>

